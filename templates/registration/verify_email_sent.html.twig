{% extends 'front/base.html.twig' %}

{% block title %}{{ "title_verify_email" | trans }}{% endblock %}

{% block main %}
    <div class="h-100 flex-column align-center justify-center gap-300">
        <p> {{ "verify_email" | trans }}</p>
        <a href="{{ path('app_verify_email_sent', {
            'uuid': userUuid,
            'resend': 'true',
        }) }}">{{ "resend_email" | trans }}</a>
    </div>
{% endblock %}
