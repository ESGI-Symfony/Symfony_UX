{% extends 'base.html.twig' %}

{% block body %}
    {{ component('header') }}

    <main class="container">
        {% block main %}{% endblock %}
    </main>

    {{ component('navbar') }}
{% endblock %}
